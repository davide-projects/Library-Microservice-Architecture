# ===========================
# SHARED CONFIGURATION - applies to all services
# ===========================

spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: false
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      properties:
        useSSL: false
        allowPublicKeyRetrieval: true

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    enabled: false
  api-docs:
    path: /v3/api-docs
    enabled: false

logging:
  level:
    root: WARN
    org.springframework: WARN
